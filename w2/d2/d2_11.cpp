#include <iostream>
#include <string>

using namespace std;

void achar(string k){
	cout << k+'a';
}

int main() {
  string k = "kuty";
  achar(k);
  // write a function that gets a string as an input
  // and appends an 'a' character to its end

  return 0;
}
